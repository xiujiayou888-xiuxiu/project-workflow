# workflow-kit 项目规则

## 项目边界

本项目只承载 `workflow-kit` repo 的设计、开发、测试、review、发布和部署。

## 项目宪法

- 本 repo 对应一个 Codex 项目，不与“全链路规划”或其他 repo 混用。
- 临时问题只建立 task，不建立项目。
- 同一项目的 task 共享 workflow-kit 背景，但每个 task 必须有独立目标。
- 需要操作其他 repo 或长期产品时，先给转派方案；用户确认后自动转派。
- 已有同目标 task 时续聊；新目标在本项目建立新 task。

## 2026-08-12 - 工具派发规则

- 来源：用户在历史任务 `07-全链路方法论｜workflow-kit开发` 的续接中明确要求。
- 之前行为：仅按任务类型描述优先能力，未区分单一工具与支持子 Agent 工具的实际派发方式。
- 新规则：支持子 Agent 且已验证独立上下文、真实并行和必要写入隔离的工具，可在并发上限内多派发互不冲突的就绪任务；单会话、不可并行或并发未知工具不预占，只按任务实际需要串行派发。竞争时按功能卡优先级、依赖满足情况和等待顺序处理。
- 原因：让工具调度反映真实并发能力，避免静态分配和无效占用。
- 范围：仅 `workflow-kit` 的工具扫描、任务分工、并行调度和执行清单。

## 2026-08-12 - 可复制派发交接包

- 来源：用户明确要求每次派发把下一个工具实际需要读取的文件整理好，而非只提供提示词或路径。
- 之前行为：每任务提示词保存文件有固定路径，但只列出需要读取的文件路径，外部工具仍要自行取数。
- 新规则：每个 `PAR-*` 必须生成独立、完整的 `[PROJECT_ROOT]\docs\并行任务\PAR-XXX-交接包.md`。总控先筛选该工具实际需要的项目资料，再将完整文本按用途、原始绝对路径和 SHA256 内嵌到交接包；无法内嵌的二进制、大型或超出上下文资料必须写明路径、哈希、获取方式和原因。需要外部 AI 接手时，聊天显示与文件完全一致的可复制代码块和绝对路径；用户可用“查看交接包 / 打开 PAR-XXX”重新查看。
- 原因：用户复制一份交接包即可让外部工具获得完成任务所需的上下文，不必自行拼接文件。
- 范围：仅 `workflow-kit` 的任务派发、执行清单、状态板和跨 AI 交接。

## 2026-08-27 - 分阶段交付路线

- 来源：用户在历史任务 `07-全链路方法论｜workflow-kit开发` 的续接中明确纠正首版规划方式。
- 之前行为：全链路容易按完整产品规划，第一版范围可能吞入后续能力；规则使用“第一版/V1”但没有把逐级升版设为默认约束。
- 新规则：所有项目默认按 `基础版 -> 进阶版 -> 高阶版 -> 最终版` 推进。基础版是可验收的正式核心闭环，不是 Demo；每个阶段只实现已确认范围，必须记录目标、包含项、明确不做、候选项、验收证据和升版决定。下一阶段须以上一阶段证据、反馈和用户确认作为前提，不能自动启动。
- 原因：避免首版一次性覆盖完整产品需求，保持每轮投入、复杂度和风险与已验证价值匹配。
- 范围：仅 `workflow-kit` 的需求澄清、产品边界、架构、功能卡、执行调度、文档模板、公开说明和版本发布材料。

## 2026-08-27 - 当前版本连续执行

- 来源：用户在历史任务 `07-全链路方法论｜workflow-kit开发` 的续接中明确纠正执行过程的确认频率。
- 之前行为：已确认开工后仍可能按步骤、功能卡、波次或可控子 Agent 任务向用户询问“下一步”。
- 新规则：当前版本的需求、范围、预算、架构和开工授权确认后，当前总控 Agent 与其可控子 Agent 连续完成已批准工作、测试、总装和验收，只做里程碑汇报和版本完成验收。再次确认仅限高风险操作、边界/验收不清，或执行中必须由用户决定的真实问题；外部不可控 AI 的用户复制、登录或授权属于实际交接确认。
- 原因：减少无意义点击和上下文中断，让用户只在真正影响范围、风险或业务决定时参与。
- 范围：仅 `workflow-kit` 的开工授权、任务执行、子 Agent 调度、外部交接、状态反馈和文档模板。
