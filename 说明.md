# AI 项目全链路总控 V3.7

给不会开发的普通人使用的项目总控 Skill。

你只需要说：

```text
我想做一个 [项目]。
```

AI 会自动：

- 先阅读已有资料并回显目标、事实、推断、风险和推荐；智能体项目每轮只询问一个真正影响决策的问题，信息足够即停止澄清并转入连续执行。
- 按基础版、进阶版、高阶版、最终版逐级交付；基础版先跑通可验收核心闭环，后续版本必须由验收证据、真实反馈和用户确认触发。
- 先区分传统 App、智能体和混合产品；智能体收到目标后主动读取授权上下文、计划、调用工具、观察和交付结果，不把中间工作退回给用户填表。
- 判断项目价值、竞品优势、可行性和完整成本。
- 正式架构和编码前，搜索成熟开源项目、官方模板、SDK、组件和设计系统，评估能否参考或复用。
- 设计成熟产品、前端、后端、数据和部署架构。
- 扫描电脑上的工具、Skill、MCP 和 Agent 能力。
- 按 A0-A4 判断单线、独立任务、工作树并行或自动总控。
- 支持子 Agent 且已验证可并行的工具可多派发；单会话工具不预占，按任务实际需要串行派发。
- 每个外部派发都有独立、完整、可直接复制的 `PAR-XXX-交接包.md`，已整理并内嵌下一个工具实际需要的文件内容；聊天同步显示同一内容和文件路径。
- 项目确认开工后自动派发能控制的子 Agent。
- 功能卡逐个开发、审查、合并和总装。
- 进行设计总监、艺术指导、成熟产品审查和超级测评。
- 测评通过后生成产品使用手册和功能说明，再按手册反向复测。
- 输出源码/URL/安装包、部署、文档、资产和支持边界，形成完整交付。

AI 发现的新功能只会先记为 `CAND-*`，未经你确认不会进入需求、报价或开发。

日常命令：

```text
下一步
项目状态
全链路
自动判断并行
优先省钱
展开执行详情
```

核心文件：

- `SKILL.md`：AI 实际执行规则。
- `docs/02-开源参考与复用评估.md`：开发前复用门禁模板和证据记录。
- 合法合规是所有项目的一票否决门禁，侵权、违法或授权/数据来源不明时不得继续采用。
- `总文档.md`：完整使用说明。
- `references/`：10 个按需加载的专业模块。

默认项目保存在 `D:\AI项目\[项目名]`，所有步骤都提供实际工具、输入、产物名称、绝对保存路径、完成标志和验收证据。
