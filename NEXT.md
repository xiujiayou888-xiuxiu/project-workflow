# workflow-kit 下一步

## 发布前

- 完成 zip 与源码结构校验。
- 提交 V3，默认分支改为 `main`。
- 用户或 Agent 通过 `gh auth login` 完成 GitHub CLI 登录。
- GitHub CLI 登录后创建公开仓库 `workflow-kit`。
- 推送 `main`，创建并推送 `v3.0.0` 标签。
- 创建 GitHub Release，并上传 `project-workflow-skill.zip` 作为发布资产。
