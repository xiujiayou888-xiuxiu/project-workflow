# 规化全链路 - V3.1 兼容入口

这是早期核心文件名的兼容入口。

V3 已把原来的长流程重构为：

- `SKILL.md`：约 300 行轻量总控内核。
- `docs/02-开源参考与复用评估.md`：需求确认后、正式架构和编码前的开源参考与复用门禁。
- `references/01-项目发现与需求.md` 至 `10-文档模板.md`：按项目阶段和风险加载的专业模块。
- `总文档.md`：给普通人看的完整使用说明。

AI 应优先读取 `SKILL.md`，根据当前阶段只读取对应模块，不要把所有参考文件一次塞入上下文。

使用方式：

```text
读取 SKILL.md，帮我做一个 [项目]。
```

旧项目继续使用原来的 `项目状态.md`、`项目路径索引.md` 和历史文档；V3 不删除、移动或重置旧代码和数据。
