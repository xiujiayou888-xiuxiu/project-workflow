# workflow-kit 决策记录

## 2026-08-03 - V3 公开开源发布

- 决策：workflow-kit V3 采用公开 GitHub 仓库发布。
- 原因：用户已明确选择公开开源，不再走低价售卖包作为主路径。
- 边界：不得上传密钥、个人数据、V2 私人归档或其他用户数据；不得删除旧目录和历史归档。

## 2026-08-03 - 许可证选择

- 决策：用户确认 workflow-kit V3 使用 MIT License 公开开源。
- 原因：MIT 简洁宽松，适合该 Skill 仓库作为公开工具分发。
- 当前处理：已补齐 `LICENSE`，README 已指向许可证文件。

## 2026-08-03 - 发布包边界

- 决策：Release 资产只上传 `project-workflow-skill.zip`。
- 原因：该 zip 只包含可安装 Skill 文件，已验证不包含个人归档、V2 私人归档、售卖包或用户数据。

## 2026-08-03 - 接入既有公开仓库

- 决策：目标仓库使用用户提供的 `https://github.com/xiujiayou888-xiuxiu/project-workflow.git`。
- 原因：该仓库已有旧版 `project-workflow v1.0` 历史，应保留而不是覆盖。
- 处理：本地 V3 与远程旧版无共同祖先，因此通过双父合并提交保留双方历史，最终文件树保持 V3，推送使用普通快进，不使用 force push。

## 2026-08-03 - V3.1 开发前开源参考与复用门禁

- 决策：作为向后兼容功能新增，将 `project-workflow` 版本提升到 `3.1.0`，新增开发前开源参考与复用门禁。
- 原因：用户明确要求项目在正式架构和编码前先查 GitHub、官方模板、包管理生态和成熟组件，避免重复造轮子，同时防止盲目复制、许可证违规、供应链风险和不合适依赖。
- 范围：根 `SKILL.md` 只负责路由和硬性门禁；详细规则放入 `references/03-架构与工程.md`、`references/04-执行与自动调度.md`、`references/08-接手项目与升级.md`、`references/09-AI产品专项.md`、`references/10-文档模板.md` 和固定产物 `docs/02-开源参考与复用评估.md`。
- 边界：不增加自动功能范围；不自动安装或执行陌生代码；付费、账号授权、许可证重大歧义、高风险软件执行或不可逆操作仍需用户确认。
- 发布规则：保留 `v3.0.0` 和旧历史，不移动旧标签，不 force push；源码、两个已安装 Skill 目录和 `project-workflow-skill.zip` 必须一致后再发布 `v3.1.0`。

## 2026-08-03 - V3.1 全项目合法合规硬门禁

- 来源任务：开发前开源参考与复用门禁追加约束。
- 决策：任何项目均须检查第三方权利、代码/内容/数据/模型授权、许可证与 NOTICE、隐私和数据来源、适用法律/行业/平台规则及违法滥用风险；R1 不能跳过该项，一旦无法确认就记录 `unknown/未完成` 并暂停采用或实现。
- 原因：开源复用评估不能只解决许可证和供应链问题，还要避免侵权、未授权数据和违法产品用途。
- 范围：根 Skill、`docs/02-开源参考与复用评估.md`、架构工程规则和交付检查模板；不扩大自动安装、执行陌生代码或自动决策范围。

## 2026-08-12 - V3.2 工具按并发能力派发

- 决策：工具调度不再只表达“优先能力”，而是按已验证的并发模型实际分配。支持子 Agent 且具备独立上下文、真实并行和必要写入隔离的工具可在上限内多派发；单会话、不可并行或并发未知工具按需串行，不预占。
- 冲突处理：多个就绪任务竞争单一工具时，按功能卡优先级、依赖满足情况和等待顺序选择；每次占用与释放都在执行清单中记录。
- 原因：避免将 A0-A4 的任务调度能力误当作任意工具都可并行使用，并保证实际工具分配可审计、可接手。
- 边界：不新增账号、付费、生产发布或自动执行陌生代码；这些动作仍需原有确认。

## 2026-08-12 - V3.4 每任务自足交接包

- 决策：每次派发采用一任务一交接包。每个 `PAR-*` 的唯一可复制内容保存为 `[PROJECT_ROOT]\docs\并行任务\PAR-XXX-交接包.md`，并同步在聊天中显示相同的完整代码块。
- 内容要求：总控先筛选下一个工具实际需要的项目资料，再将完整文本内嵌到交接包，每段保留原始绝对路径、SHA256 和用途；二进制、大型或超出上下文限制的资料必须明确获取方式和未内嵌原因。
- 查看方式：执行清单、状态板和聊天均显示该文件绝对路径；用户可通过“查看交接包 / 打开 PAR-XXX”重新调出。
- 原因：让外部 AI 接手时获得可直接使用的完整上下文，避免用户和工具自行串联原项目文件。
- 边界：可控的内置 Agent 仍可自动启动；交接包用于查看、审计和无法自动控制的外部派发，不伪造无法内嵌的资料。

## 2026-08-27 - V3.5 分阶段交付路线

- 决策：将全链路默认交付模式固定为 `基础版 -> 进阶版 -> 高阶版 -> 最终版`。基础版必须交付一个真实、可用、可验收的核心闭环；它不是 Demo。每一阶段都独立定义范围、明确不做、候选项、验收证据和升版条件。
- 升版规则：完成当前阶段后先生成复盘与升版决定。只有上一阶段验收、真实反馈或业务证据成立，且用户明确确认新的范围，才创建和派发下一阶段需求与功能卡。
- 原因：产品路线图不等于首版实施范围；渐进交付降低首版复杂度、成本和返工风险，并让投入建立在已验证价值上。
- 边界：`R1-R4` 仍决定治理和风险控制，不能因阶段较小而跳过适用的安全、合规、审批或 POC 门禁；不自动发布、不改变既有已发布版本。

## 2026-08-27 - V3.6 当前版本连续执行

- 决策：当前版本获得一次开工授权后，当前总控 Agent 与实际可控制的子 Agent 连续完成已批准的工作、测试、总装和验收；不再按步骤、功能卡、波次或可控子 Agent 任务向用户重复索要“下一步”。
- 再次确认边界：仅高风险操作、范围/共享契约/验收/预算无法明确，或执行中出现必须由用户作业务决定的真实阻塞；外部不可控 AI 需要用户复制交接包、切换工具或登录授权时，在实际交接点确认。
- 原因：已批准工作应由总控负责推进，用户只参与会改变范围、风险、成本或业务判断的决定。
- 边界：不削弱合法合规、付费、账号、密钥、生产发布、数据迁移、删除和不可逆操作的单独确认要求；当前版本完成后仍等待用户验收和升版决定。

## 2026-08-27 - V3.7 智能体产品形态与主动任务循环

- 决策：将产品形态判定加入需求确认硬门槛：`传统 App / 智能体 / 混合产品`。判定为智能体或混合产品时，必须加载 AI 专项，创建 `AI-FLOW-*` 主动任务契约，并按目标触发、主动上下文、计划、工具行动、观察修正、结果证据和人工接管设计。
- 验收：智能体以任务完成率、人工介入率、正确工具行动率、主动获取上下文成功率、结果质量和安全拦截作为核心证据；页面、聊天框或表单可提交不构成智能体完成。
- 原因：用户要求智能体主动做事、提供结果与下一建议，而非把每个中间步骤、上下文收集和执行动作退回给用户。
- 边界：高风险行动、边界不清和必须由人作业务判断的事项仍暂停确认；不能自主推进的产品必须诚实称为助手、表单工作流或传统 App。

## 2026-08-27 - V3.7 智能体主动与定时运行

- 决策：智能体不按“一问一答”定义。每个主任务须声明用户目标、授权事件或定时计划触发，并在已批准边界内自主读取状态、行动、保存结果、主动汇报和提醒。
- 运行契约：定时/事件任务必须有频率与时区、触发条件、状态持久化、允许行动、预算、通知、人工暂停/取消、幂等、超时、重试和停止条件；运行前检查任务有效性、权限、上下文时效、预算和暂停状态。
- 原因：让智能体在用户不发新消息时仍能持续推进明确目标，同时可控、可审计、可停止。
- 边界：高风险写操作、付款、发布、删除、权限扩大和不可逆行动不因事件或定时触发而自动执行，仍需单独确认。

## 2026-08-27 - V3.7 先读后问的智能体澄清

- 决策：把 GitHub `Grill me` 类项目的“先研究、再提出关键决策”方法转化为本项目的智能体澄清短循环：读取现有资料和已授权上下文，生成可纠正的理解卡，区分事实/推断，主动给出风险、替代方案和推荐；每轮只提出一个影响目标、范围、验收、风险或授权的决策问题。
- 验收：需求记录包含理解卡、唯一高影响问题、推荐、用户决定与停止澄清依据；达到目标、当前阶段边界、验收、权限和风险可执行阈值后，不再追问，进入当前版本连续执行。
- 原因：保留主动理解和高质量决策，而不是把用户变成问卷填写者；这与“开工确认后不逐步确认”的现有规则一致。
- 边界：只借鉴交互方法，不复制第三方代码、文案或品牌；高风险操作、边界不清和真实业务判断仍必须单独确认。
