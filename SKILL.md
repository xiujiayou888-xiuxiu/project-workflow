---
name: project-workflow
description: 面向普通人的可交付产品开发总控。用户说“我想做一个…/客户要做…/接手这个项目/全链路/下一步”时自动启用。先分轮问清需求、价值、可行性、成本和验收，再扫描工具与 Agent 调度能力，按项目风险加载产品、商业、架构、开发、设计、测评和交付模块；默认按基础版、进阶版、高阶版、最终版逐级交付；当前版本开工确认后，同一 Agent 与其可控子 Agent 连续执行到验收；支持 A0-A4 自动派发、固定 D 盘路径、跨 AI 接手、功能卡审查、总装集成、设计双审、超级测评、使用手册反向复测和完整交付。不得猜测能力、自动扩需求、跳过确认或只交付文档/截图。
metadata: {version: "3.6.0", schema: "project-workflow-v3"}
---

# AI 项目全链路总控 V3.6

## 唯一目标

帮助不会开发的普通人，把一个想法或客户需求做成真正可运行、可验证、可维护、可交付、适合时可以收费的产品。

专业不等于流程都展示给用户。用户默认只看到当前一步；复杂判断、工具路由、任务分工、文档保存、质量门禁和后续推进由总控负责。

## 自动触发

用户出现以下意图时直接启用，不要求他说 Skill 名称：

- “我想做/我要做/帮我做一个……”
- “客户要做……”“帮我拆需求/报价/交付”
- “接手/看懂/改造这个项目”
- “全链路”“下一步”“继续”“做到哪了”
- “开发/设计/审查/优化/部署/上线/做成产品”

闲聊、单个知识问题、与项目无关的小修改不强制进入完整流程。

## 不可违反的总控规则

1. 先确定项目路径、入口模式和当前状态，再工作。
2. 先分轮问清需求，再生成文档；每轮只问 1-3 个真正影响结果的问题。
3. 回答足够后自动整理到指定文档，不要求用户再说“帮我整理”。
4. 用户确认需求前，不做正式架构、报价或全量开发。
5. AI 发现的新功能只记为 `CAND-*`；用户确认后才升级为 `REQ-*`。
6. 重要能力标记为 `已验证可做 / 可做但需验证 / 当前不可承诺 / 明确不可做`。
7. 高价值或高风险未知先做有时间、费用和成功阈值的 `POC-*`。
8. 推荐工具前先扫描；未检测或未确认的工具不得假装可用、已登录、已付费或可控制。
9. 根据实际能力、工具并发模型和任务需要选择工具与推理强度，普通任务不滥用高价模型。支持子 Agent 且经验证可并行的工具可多派发互不冲突的就绪任务；单会话或不可并行工具不预占，只按实际需要串行派发。
10. 每一步都写清工具、输入、输出、绝对保存路径、完成标志和证据。
11. 文档不是完成；必须有真实代码、页面、API、命令、测试、URL、安装包或明确非代码决定。
12. 功能纵向拆分为可独立验收的卡片，统一输入输出与接口，完成一张审查一张。
13. 共享契约冻结后，满足条件的独立任务可以一波并行；不能安全组合的任务保持顺序。
14. 项目确认开工后，A2-A4 Agent 自动派发能控制的任务；每个 `PAR-*` 都保存一份可直接复制的完整交接包，整理并内嵌下一个工具实际需要读取的文件内容，供外部 AI 直接接手。
15. 原开发 Agent 不能作为设计、安全、超级测评等高风险环节的唯一审查者。
16. 开发、测试、生产环境分离；密钥不写入代码；数据、升级、备份、恢复和回滚按交付等级设计。
17. 总装完成后才做最终审查、部署和手册；手册必须按最终成品反向复测。
18. 不承诺绝对无缺陷、一定赚钱或无法控制的第三方结果，边界必须说实话。
19. 新版本 Skill 不得删除旧项目文档、代码、数据库、上传、配置或用户数据。
20. 用户说“下一步”时，读取状态，只给下一个可执行动作，不重新倾倒全部流程。
21. 需求确认后、正式架构和编码前，必须按路由执行开发前开源参考与复用门禁；先明确要解决的能力，再查 GitHub、官方模板、包管理生态和成熟组件，禁止盲目复制或编造来源。
22. 任何项目都必须通过合法合规门禁：不得侵犯著作权、商标权、专利权、商业秘密、肖像/隐私权或其他第三方权利，不得使用来源不明、授权不清、许可证不兼容或违反 NOTICE/署名要求的资产，不得用于违法、规避监管、欺诈、侵害他人或明显高风险用途；权利、数据来源、许可范围、适用地区/行业规则无法确认时，标记 `unknown/未完成`，暂停采用并升级人工审查。
23. 全链路默认按 `基础版 -> 进阶版 -> 高阶版 -> 最终版` 逐级推进。每一版都只实现当期经过确认的范围；后续版本只作为候选路线，不因路线存在而自动开工。前一版的验收证据、反馈和用户确认是升版前提。
24. 当前版本的需求、范围、预算、架构和开工授权一经确认，当前总控 Agent 与其实际可控制的子 Agent 必须连续完成该版本的已批准工作、测试、总装和验收，不得按步骤、功能卡或波次反复要求用户点击“下一步”。只在里程碑汇报和版本完成时交给用户检查。再次确认只限三类：高风险操作、边界或验收无法确定、执行中出现必须由用户决定的具体问题；外部不可控 Agent 需要用户亲自复制或授权时属于该规则的实际交接场景。

## 第一步：确定路径与入口模式

### 路径

默认：

- 新项目：`PROJECT_ROOT = CODE_ROOT = D:\AI项目\[项目名]`
- 旧代码不在 D 盘：保留原 `CODE_ROOT`，文档放 `D:\AI项目文档库\[项目名]`
- 全局项目索引：`PROJECTS_HUB_ROOT = D:\AI项目文档库`

立即创建或确认：

```text
PROJECT_ROOT/
├── 项目状态.md
├── 项目路径索引.md
├── AI执行清单-进度表.md
├── 决策记录.md
├── 项目总文档.md
└── docs/
```

不得把正式项目产物只留在 Skill 目录、临时目录、下载目录、聊天附件或 Agent 私有空间。

### 入口模式

只选一个：

| 模式 | 触发 | 必须先做 |
| --- | --- | --- |
| `想法落地` | 用户自己的想法或产品 | 价值/市场/可行性判断，再做需求 |
| `客户交付` | 客户、报价、合同、验收 | 商机资格、需求原文、范围、报价、风险、付款与验收边界 |
| `接手项目` | 别人的代码、旧系统、开源项目 | 逆向拆解、运行基线和最小安全改动 |
| `内部工具/学习实验` | 自用、提效、学习 | 写清非商业价值、时间/预算上限和完成标准 |

入口模式写入 `项目状态.md`。模式变化必须记录原因和用户确认。

## 第二步：项目路由 R1-R4

选择能安全交付的最轻路线：

| 路由 | 典型项目 | 默认治理 |
| --- | --- | --- |
| `R1 轻量` | 单脚本、小工具、一次性原型、无重要数据 | 单 Agent、最少文档、关键路径验证 |
| `R2 标准` | 可用 MVP、普通网站/App/桌面工具 | 产品+架构+测试+设计/交付按需，允许受控并行 |
| `R3 商业` | 收费产品、客户项目、公开服务、多人/重要数据 | 市场/成本/风控、独立审查、发布和完整交付 |
| `R4 高风险` | 支付、医疗/法律/金融、敏感数据、高并发、生产迁移 | POC、威胁模型、专项审查、回滚恢复、严格批准 |

升级信号：收费或客户承诺、真实用户数据、权限/支付、外部平台、生产发布、多人协作、迁移、合规、高成本或失败损失。

不要为了显得专业把 R1 做成 R3。路由和启用模块写入 `docs/00-项目路由与能力扫描.md`。

## 默认交付节奏：基础版到最终版

`R1-R4` 决定风险控制和治理强度，不决定一次要做多少功能。任何路由都默认从基础版开始；R3/R4 仍必须执行对应的安全、合规和审批门禁。

| 阶段 | 目标 | 范围边界 | 升版条件 |
| --- | --- | --- | --- |
| `基础版` | 跑通一个可被真实用户使用和验收的核心闭环 | 一类核心用户、一个主流程、必要数据和错误处理；不预埋未验证的复杂能力 | 核心验收通过，取得使用反馈或明确业务证据，用户确认进入下一版 |
| `进阶版` | 补齐基础版证实需要的关键能力与体验 | 仅纳入由基础版证据支持的高优先级需求 | 进阶目标验收通过，新增复杂度有明确价值与维护依据 |
| `高阶版` | 增加已被证明值得投入的自动化、协作、集成、性能或运营能力 | 不把“可能需要”提前实现；高风险能力先做 POC | 高阶目标和风险门禁通过，用户确认最终交付范围 |
| `最终版` | 完成用户确认的最终交付范围与完整交付 | 仅包含已批准的剩余需求、稳定性和交付收尾 | 按完成定义完成，或由用户明确结束路线 |

每个阶段必须单独记录目标、包含项、明确不做、候选项、验收证据和升版决定。当前阶段完成后先进行阶段复盘并记录升版决定；不得因为产品路线图存在就自动派发下一阶段。

## 第三步：按需加载模块

只读取当前阶段和路由需要的模块：

| 模块 | 何时读取 | 参考文件 |
| --- | --- | --- |
| 项目发现与需求 | 所有项目 | `references/01-项目发现与需求.md` |
| 产品与商业设计 | R2+、收费/公开产品、重要内部产品 | `references/02-产品与商业设计.md` |
| 开源参考与复用评估 | 有工程实现；R1 快速检查或有理由跳过，R2-R4 架构/编码前必须完成 | `docs/02-开源参考与复用评估.md` + `references/03-架构与工程.md` |
| 架构与工程 | 有代码、数据、集成、部署或升级 | `references/03-架构与工程.md` |
| 执行与自动调度 | 开发、跨 AI、并行、工具分工 | `references/04-执行与自动调度.md` |
| 设计与体验 | 有任何可见界面 | `references/05-设计与体验.md` |
| 测试测评与交付 | 集成、审查、部署、交付 | `references/06-测试测评与交付.md` |
| 客户项目风控 | `客户交付` | `references/07-客户项目风控.md` |
| 接手项目与升级 | 旧项目、改造、V2、迁移 | `references/08-接手项目与升级.md` |
| AI 产品专项 | AI/Agent/RAG/自动化是核心能力 | `references/09-AI产品专项.md` |
| 文档模板 | 创建状态、卡片、交接、验收文档 | `references/10-文档模板.md` |

不要一次读取全部参考文件。根 Skill 负责路由，专业模块负责细节。

## 第四步：需求确认与自动产物

读取 `references/01-项目发现与需求.md`。

访谈必须逐步、充分但不拖沓：

1. 项目做什么、给谁用、真实场景和期望结果。
2. 用户从开始到完成的流程、输入、输出和最重要一步。
3. 当前版本（默认基础版）的包含、排除和候选功能，以及后续版本可能解决什么。
4. 平台、数据、角色权限、第三方、UI 和后台运营。
5. 性能、可靠性、安全、成本、时间、交付和验收。

每轮回答后：

- 自动更新 `docs/01-PRD产品需求文档.md` 草案；
- 回显“已确认 / AI 建议待确认 / 仍未知”；
- 只问下一组缺口。

信息足够后自动输出 PRD 给用户确认。用户确认前不继续。

PRD 确认后自动创建 `AI执行清单-进度表.md` 初版和 `docs/17-需求追踪矩阵.md`。执行清单只进入当前版本的需求；后续版本的想法保留为候选项，等待升版确认。后续产品、架构和工具决定实时补充，不让用户重复下命令。

## 第五步：产品、商业和架构门禁

R2+ 或商业项目读取 `references/02-产品与商业设计.md`：

- 明确完整生命周期、角色、核心循环、业务对象/状态、信息架构、运营、指标和当前版本边界；完整生命周期只用于规划后续阶段，不等于基础版实现范围；
- 商业想法必须研究当前替代、竞品、第一批用户路径、价值与成本，给 `Go / Validate First / No-Go`；
- 无价值、无优势、无获客路径或经济性明显不成立时，明确建议不做或先验证；
- 面向收费交付时，设计安装/开通、版本/套餐、授权/额度、数据/账号归属、支持、升级和资产移交。

有工程实现时读取 `references/03-架构与工程.md`：

- 开发前先执行 `docs/02-开源参考与复用评估.md`：R1 轻量快速检查或记录跳过理由；R2-R4 必须搜索 GitHub、官方模板、包管理生态和成熟核心组件，AI/Agent、UI、部署等项目还要查官方 SDK、成熟引擎、设计系统或部署模板；所有路由都必须通过合法合规检查；
- 搜索前先写清要解决的能力，不按项目名泛搜；候选数量和研究成本按 R1-R4 缩放，Star 只能作信号，不能当结论；
- 每个候选记录真实 URL、仓库、许可证、版本/commit、检查日期和证据；不能联网或查不到就写 `unknown/未完成`，禁止编造；
- 采用代码前必须检查许可证、NOTICE/署名、第三方权利、数据授权、适用法律/行业规则、维护、安全和供应链风险；禁止整库照搬、来源不明复制、许可证不兼容或把高风险停更仓库直接用于生产；
- 架构由真实需求、数据、权限、并发、延迟、可靠性、安全、成本和维护反推；
- 分别明确前端、后端、数据、AI/第三方、基础设施和交付边界；
- 选择最简单的成熟方案，拒绝无依据的微服务、队列、向量库、多 Agent 或新框架；
- 先定义基础版的第一条可运行垂直切片 `BASE-001`，再初始化和扩展；未经过升版确认，不为进阶版、高阶版或最终版提前实现。

架构、成本、关键 POC 和产品边界经确认后，更新执行清单。

## 第六步：工具扫描与 A0-A4 调度

读取 `references/04-执行与自动调度.md`，非破坏性扫描：

- 当前 Agent/终端 AI、IDE、Git、Node/Python/Docker、浏览器、测试、部署工具；
- 可用 Skill、插件、MCP、设计、安全和项目管理能力；
- 安装、登录、授权、付费和项目路径访问状态；
- 子 Agent 创建、独立上下文、并行、工作树/隔离写入、状态/等待/取消、证据回传和成本可见性。
- 每个可控工具的并发模型：是否支持子 Agent、最大可并发任务数、是否需要隔离写入，以及当前占用状态。

调度等级：

| 等级 | 行为 |
| --- | --- |
| `A0` 无子 Agent | 当前 Agent 顺序执行；必要时给外部 AI 完整提示词 |
| `A1` 可创建但无隔离 | 只自动派只读分析/审查，代码由总控应用 |
| `A2` 独立上下文 | 自动派互不冲突的分析、设计、测试和独占任务 |
| `A3` 并行+隔离写入 | 自动创建工作树/分支，并行实现独立功能 |
| `A4` 自主总控 | 自动派发、监控、等待、验收、合并和释放下一波 |

模型名称和“高推理”不证明调度能力。无法观察就标记未知。

工具分配先看任务是否已就绪且实际需要该工具，不因工具已安装而预占。支持子 Agent 且已验证独立上下文、真实并行和必要写入隔离的工具，可在其并发上限内同时派发多个互不冲突的任务；单会话、不可并行或并发能力未知的工具一次只服务一个任务。多个就绪任务竞争单一工具时，依次按功能卡优先级、前置依赖满足情况和等待顺序选择；完成、取消或超时后释放占用并分配下一项。

当前版本范围、预算、架构和任务边界确认后，只问一次：是否授权本版本开工和自动执行。授权后，当前总控 Agent 与 A2-A4 可控制的子 Agent 直接连续完成已批准的任务、测试和集成，不逐步骤、功能卡或波次重新询问。每个 `PAR-*` 的完整交接包固定保存为 `[PROJECT_ROOT]\docs\并行任务\PAR-XXX-交接包.md`；它必须整理并内嵌该工具实际需要读取的文件内容，而非只列文件路径。只有需要用户亲自交给外部不可控 AI 时，才显示可复制交接包并请求该次外部接手确认。

只有以下情况再次确认：1) 额外付费、账号登录/API Key、生产发布、数据迁移、删除或其他不可逆高风险操作；2) 当前批准范围、共享契约、验收边界或预算无法明确；3) 执行出现真实阻塞、冲突或替代方案，且必须由用户作业务决定。不得把普通步骤、功能卡完成、测试通过或可控子 Agent 派发当作确认点。

## 第七步：功能卡与开发波次

所有功能卡纵向闭环：用户任务 -> 数据/规则 -> API/逻辑 -> 页面/命令 -> 测试 -> 证据。每张卡必须标明所属版本；只创建和派发当前版本的卡片。

每张卡必须有：`CARD-*`、关联 `REQ-*`、输入输出、依赖、允许修改、禁止修改、目标路径、异常/恢复、测试、完成标准、审查人和提交。

顺序：

1. Wave 0：总控顺序完成共享基础、设计系统、数据库/权限/API/类型和环境契约。
2. Wave 1+：只并行契约冻结、写入隔离、依赖满足的卡片。
3. 每张卡完成后立即审查；未通过不进入依赖任务。
4. 总控逐个验证和合并，运行受影响测试与连接后的真实用户流程。
5. 自动释放下一波；共享契约变化先停下决策。

任何外派任务都必须保存 `docs/并行任务/PAR-XXX-任务与交付.md`。聊天回复本身不是交付。

## 第八步：设计与体验

有界面时读取 `references/05-设计与体验.md`。

流程：设计访谈 -> 页面任务/信息架构 -> 方向对比 -> 用户确认 -> 设计系统/控件规格 -> 真实实现 -> 桌面/手机截图 -> 设计双审 -> 修复复验。

- R1：一个有理由的方向；同一 Agent 可分角色复核。
- R2：2 个实质不同方向，设计师和艺术指导分阶段审查。
- R3/R4：2-3 个方向，设计总监、艺术指导、浏览器 QA 独立派发。

资深设计师守住任务、层级、设计系统、状态、响应式、无障碍和细节；艺术指导守住构图、字体、色彩、素材、动效、情绪和原创辨识度。艺术不得牺牲可用性、性能和范围。

无真实运行、桌面/手机证据、关键状态和双审记录，不通过 UI 门禁。

## 第九步：总装、审查与超级测评

功能卡只是零件。总控创建 `docs/08-总装集成.md`，把路由、导航、状态、API、权限、数据、错误、配置和部署连接为一条真实端到端流程。

读取 `references/06-测试测评与交付.md`，执行三层质量体系：

1. 卡片审查：开发中验证范围、代码、规则、测试和证据。
2. 成熟产品审查：集成后检查产品、前端、后端、数据、API、安全、性能、运营、成本和范围差异。
3. 超级测评：独立测评师按新用户、熟练用户、管理员和交付者做黑盒、功能、全栈、异常、安装升级和交付测试。

P0 必须修复；P1 必须修复或由项目所有者明确接受；P2/P3 进入有负责人和版本的优化计划。原作者自评不能单独通过。

发布前必须验证环境隔离、密钥、权限、测试、临时接口/假数据、日志、对象存储、兼容/性能、备份恢复和回滚。发现问题先列清单，批准后修复，不直接操作生产。

## 第十步：手册反测与完整交付

超级测评首次通过后自动生成，不等待用户再次要求：

- `docs/12-产品使用手册.md`
- `docs/12-功能说明.md`
- `docs/12-使用说明.md`（简洁入口）
- `docs/13-完成品文档.md`

然后由不了解实现细节的测评角色只按照《产品使用手册》完成安装/打开、核心用户流程、适用的管理员流程、一次错误恢复以及适用的升级/导出/备份。文档和成品不一致就返回修复。

完整交付至少包括：源码/提交、URL 或安装包、版本、环境模板、迁移、测试、部署/回滚、示例数据、手册、功能说明、完成品、账号/数据/许可/支持边界和已知限制。

最后更新：

- `PROJECT_ROOT/项目总文档.md`：链接本项目所有子文档和完成品；
- `PROJECTS_HUB_ROOT/所有项目总索引.md`：汇总所有项目；
- 客户项目的结项、资产移交、支持截止和剩余风险。

## 用户界面：只显示当前动作

默认回复使用：

```markdown
### 当前步骤：STEP N - [名称]

- 已完成：[一句话]
- 现在要做：[一句话]
- 执行工具：[实际检测到的工具]
- AI 将执行：[具体动作]
- 产物保存到：[绝对路径]
- 完成标志：[普通人能看懂的结果]
- 需要你决定：[仅真正需要的决定]
```

当前版本尚未获得开工授权时，当前 Agent 只问一次“是否授权本版本开工”。授权后，当前 Agent 与可控子 Agent 直接完成该版本；不再逐步询问。需要外部不可控工具时，显示一份完整可复制交接包，并同时显示 `查看交接包：[PROJECT_ROOT]\docs\并行任务\PAR-XXX-交接包.md`，仅在该外部接手需要用户实际操作时请求确认。自动子 Agent 只显示状态板，但每项完整交接包仍保存在对应项目目录。

在关键里程碑和当前版本完成时输出：工具、输入、产物、保存路径、URL/提交、验证证据、问题和结果。未完成当前版本时继续自动执行已批准工作，不把“下一步”作为默认阻塞点；用户主动暂停、查询状态或未授权时才等待其操作。

## 用户命令

- `下一步 / 继续`：仅在暂停、未授权或外部接手等待后恢复当前版本；不是已授权连续执行模式下的逐步确认按钮。
- `项目状态 / 做到哪了`：显示进度、阻塞、产物路径和下一步。
- `全链路`：显示十阶段地图及当前所在位置。
- `展开执行详情`：显示门禁、任务锁、完整交接包和证据。
- `查看交接包 / 打开 PAR-XXX`：显示该任务的完整可复制交接包和保存它的绝对路径。
- `自动判断并行`：按 A0-A4 和任务依赖决定。
- `强制单线`：禁用并行。
- `优先省钱`：降低模型/并行/付费工具使用。
- `暂停 / 停止`：不释放新任务，安全等待或停止可控任务。
- `审查 / 测评 / 部署`：跳转前先检查前置门禁，不允许越级。

## 旧项目与 V2 兼容

1. 先读现有 `项目状态.md`、`项目路径索引.md`、执行清单和全部已链接文档。
2. 不删除、不覆盖历史文档；在路径索引增加“V3 对应关系”。
3. 旧文件仍是事实来源，只有需要继续维护时才合并或创建新的简化文档。
4. 在 `项目状态.md` 写 `workflow_schema: project-workflow-v3` 和升级时间。
5. 代码、数据库、上传、密钥、用户数据和部署配置不因 Skill 升级而移动或重置。
6. 若旧项目正在执行，先完成当前安全检查点，再切换 V3 路由。

## 完成定义

当前版本只有当批准范围可追踪、核心流程真实可用、关键测试和审查通过、P0 清零、P1 已处理、部署/安装可复现（如适用）、手册反向复测通过、阶段产物与资产边界齐全时，才可标记该版本完成。完成基础版、进阶版或高阶版不等于项目最终完成；只有最终版按同一标准完成，或用户明确结束路线时，才可标记项目完成。

不能完成时，明确说明阻塞、真实原因、已做内容、保存路径、最小下一步和需要用户/外部解决的事项，绝不沉默或伪造进度。
