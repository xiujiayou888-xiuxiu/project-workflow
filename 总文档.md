# AI 项目全链路总控 V3.1 - 总文档

## 这是什么

这是一个给普通人使用的 AI 项目总控 Skill。

你不需要先会产品、设计、编程、架构、测试或部署。你只要说清楚想做什么、回答关键问题、确认重要决定，AI 就会把项目一步步推进成真正可运行、可验收、可交付、适合时可以收费的产品。

它不是代码生成提示词合集，而是一个项目操作系统：

```text
想法/客户需求
-> 分轮访谈
-> 需求确认
-> 可行性、价值与成本
-> 开源参考与复用评估
-> 成熟产品和架构
-> 工具/Agent 能力扫描
-> 自动开发分工
-> 逐功能审查和总装
-> 设计双审与超级测评
-> 手册反测
-> 完整交付
```

## 最适合谁

- 想用 AI 做软件但不会开发的人。
- 想把个人想法做成可收费产品的人。
- 接客户项目，需要控制需求、报价、风险和验收的人。
- 同时使用 Codex、Claude、Cursor、ZCode 等工具，不知道如何分工的人。
- 需要多个 AI 可以跨会话、跨工具、无聊天上下文接手的人。

## 最简单的用法

安装后直接说：

```text
我想做一个 [项目]。
```

或：

```text
客户想做一个 [项目]，帮我判断能不能接、怎么报价和交付。
```

或：

```text
接手这个项目，先帮我看懂，不要直接改代码。
```

Skill 会自动触发。不要一次把所有技术决定都想好，AI 会每轮只问 1-3 个关键问题，并把答案自动整理进项目文档。

## 用户看到的十步

1. 确定项目位置、入口模式和当前状态。
2. 分轮了解用途、用户、流程、输入输出、功能、约束和验收。
3. 自动生成并确认《PRD产品需求文档》。
4. 判断价值、竞品、优势、可行性、成本和商业边界。
5. 开发前搜索成熟开源项目、官方模板、SDK、组件和设计系统，判断能否参考或复用。
6. 完成成熟产品设计和前后端/数据/部署架构。
7. 扫描本机工具和 Agent 自动调度能力，生成执行清单。
8. 用户确认开工，AI 自动派发适合的开发任务。
9. 功能卡逐个开发、审查、合并、总装并完成测评。
10. 自动生成手册、按手册反向复测并完整交付。

用户默认只看到当前一步，不会被几十页流程淹没。

## 两类核心项目

### 自己的想法

AI 会先判断：

- 是否真的解决明确问题。
- 有没有市场或内部价值。
- 当前替代和竞品是什么。
- 是否有可信优势和第一批用户路径。
- 技术上能否实现、需要验证什么。
- 开发和持续运行成本是多少。

结论是 `Go / Validate First / No-Go`。没有价值或证据时，AI 要明确劝停或建议做最小验证，不能因为能写代码就浪费用户时间和钱。

### 客户项目

AI 不会先免费给完整方案，而是依次做：

```text
聊天澄清 -> 商机评分 -> 一页说明 -> 正式需求 -> 范围/报价/风险 -> 开工条件 -> 交付
```

范围、验收、客户责任、修改次数、第三方费用、付款/开工条件和支持边界不清楚时不正式开发。

## R1-R4 路由

| 路由 | 项目 | 处理方式 |
| --- | --- | --- |
| R1 | 小脚本、简单工具、原型 | 单 Agent、最少文档、快速验证 |
| R2 | 标准 MVP、普通网站/App | 产品、架构、测试和交付按需启用 |
| R3 | 收费产品、客户项目、公开服务 | 商业、设计、独立审查和完整交付 |
| R4 | 支付、敏感数据、高风险生产 | POC、安全、恢复、专项审查和严格批准 |

简单项目不会被迫走商业大项目流程；收费和真实用户项目也不会被当成 Demo。

## A0-A4 自动调度

Skill 先识别当前 Agent 是否真正能创建、控制和监控子 Agent：

| 等级 | 能力 | 开工后行为 |
| --- | --- | --- |
| A0 | 无子 Agent | 当前 Agent 单线做；必要时给跨 AI 提示词 |
| A1 | 可创建但无隔离 | 子 Agent 只做只读分析/审查 |
| A2 | 有独立上下文 | 自动派独立任务 |
| A3 | 可并行并隔离写入 | 使用工作树/分支并行开发 |
| A4 | 可派发、监控、等待、验收、合并 | 自动完成批准波次 |

需求、架构、预算和任务范围确认后，只问一次是否开工。授权后，A2-A4 自动创建并派发能控制的任务，不要求你逐条复制内部提示词。

提示词和交付记录仍保存到项目目录，便于追踪、失败接管和下一 AI 读取。

只有以下情况会重新问你：新增需求、增加费用、登录授权、生产发布、真实数据/破坏性操作、共享架构变化或预算超限。

## 专业角色怎样使用

永久角色只有一个：项目总控。

产品经理、架构师、工程师、资深设计师、艺术指导、安全专家和超级测评师都是按风险加载的专业视角：

- R1 可以由同一 Agent 分阶段完成。
- R2 关键审查用独立阶段或会话。
- R3/R4 把设计、安全、测评等高风险角色独立派发。

资深设计师负责可用性、设计系统、状态、响应式、无障碍和细节；艺术指导负责构图、字体、色彩、素材、动效、风格和原创性；超级测评师负责黑盒使用、功能、全栈、异常、安装升级和交付。

## 不会自动乱加功能

AI 发现有价值的补充功能，只记录为 `CAND-*` 候选项，写清依据、价值、成本、风险和验证方式。

只有用户确认后才会成为 `REQ-*`，进入 PRD、报价、架构和开发。竞品有、AI 能做、看起来高级都不是自动增加功能的理由。

## 开发前先查可复用资产

需求确认后、正式架构和编码前，Skill 会先明确项目要解决的能力，再搜索 GitHub、官方模板、包管理生态和成熟组件。AI/Agent、UI、部署等项目还会查官方 SDK、成熟引擎、设计系统或部署模板。

结果固定保存到 `docs/02-开源参考与复用评估.md`。每个候选必须有真实 URL、仓库或包名、许可证、版本/commit、检查日期和证据；不能联网或查不到就写 `unknown/未完成`。最终明确选择 `直接采用 / 组合组件 / Fork 改造 / 只参考架构或交互 / 做 POC / 完全自研`。

这不是“看到开源就复制”。采用代码前必须检查许可证、NOTICE/署名、维护、安全、供应链、升级和替换成本；还要确认代码、内容、数据、模型和媒体的权利来源及适用法律/行业规则。任何项目不得侵权或违法，疑点未解决时不得采用；禁止整库照搬、来源不明复制、许可证不兼容或把停更高风险仓库直接用于生产。

## 文档不会无限膨胀

所有项目始终保留核心入口：

```text
项目状态.md
项目路径索引.md
AI执行清单-进度表.md
docs/01-PRD产品需求文档.md
docs/02-开源参考与复用评估.md
docs/12-产品使用手册.md
docs/12-功能说明.md
docs/13-完成品文档.md
项目总文档.md
```

R1 合并产品、架构、开发和审查记录；R2 按阶段增加；R3/R4 才拆出市场、成本、客户风控、安全、发布和专项测评文档。

一个事实只保存在权威文档，其他文档用路径和 ID 引用，减少重复和冲突。

## 项目文件默认放哪里

- 新项目：`D:\AI项目\[项目名]`
- 旧代码不在 D 盘：代码原地保留，文档放 `D:\AI项目文档库\[项目名]`
- 全部项目索引：`D:\AI项目文档库\所有项目总索引.md`

每个跨 AI 提示词都带 `PROJECT_ROOT`、`CODE_ROOT`、输入路径、输出路径和完成回报。产物不能只留在聊天、临时目录或 Agent 私有工作区。

## 一个功能怎样开发

每张功能卡都是完整纵向闭环：

```text
用户任务 -> 数据和规则 -> API/逻辑 -> 页面或命令 -> 测试 -> 证据 -> 审查 -> Git 提交
```

共享数据库、权限、API、类型、路由、设计系统和环境配置先由总控冻结。独立功能可以并行，但必须使用独占文件/目录或工作树，最后由总控逐个验收和合并。

## 什么叫真正有设计

不是套模板或加渐变，而是：

- 先问真实用户、任务、内容、设备、品牌和审美偏好。
- R2+ 比较真正不同的视觉方向后再实现。
- 统一颜色、字体、间距、组件、图标、素材、动效和响应式。
- 每个按钮、输入、菜单和危险操作都有规则、状态和恢复。
- 用真实项目、真实内容、桌面和手机截图审查。
- 设计总监审可用和细节，艺术指导审风格与辨识度。

没有真实 URL/截图、关键状态或设计双审，不算 UI 完成。

## 三层质量体系

1. 功能卡审查：开发中立即发现范围、规则、权限、代码和测试问题。
2. 成熟产品审查：总装后检查产品、前后端、数据、API、安全、性能、运营、成本和范围差异。
3. 超级测评：独立角色按新用户、熟练用户、管理员和交付者真实使用。

P0 必须修复；P1 必须修复或由项目所有者明确接受；P2/P3 进入有负责人和版本的优化计划。

## 完整交付

超级测评通过后自动生成：

- 《产品使用手册》
- 《功能说明》
- 《使用说明》入口
- 《完成品文档》

然后由不了解实现的人只按照手册完成安装、核心流程、管理员流程、错误恢复和适用的升级/导出/备份。手册与成品不一致就修复重测。

完整交付包含源码/提交、URL 或安装包、版本、环境模板、迁移、测试、部署/回滚、示例数据、手册、功能说明、完成品、账号/数据/许可/支持和已知限制。

## 常用命令

- `下一步 / 继续`：推进当前动作。
- `项目状态 / 做到哪了`：显示进度、阻塞、路径和下一步。
- `全链路`：显示十步地图和当前位置。
- `展开执行详情`：显示门禁、任务锁、提示词和证据。
- `自动判断并行`：按 A0-A4 自动选择。
- `强制单线`：禁用并行。
- `优先省钱`：减少高价模型和付费工具。
- `暂停 / 停止`：不释放新任务。

## 文件结构

```text
workflow-kit/
├── SKILL.md                 # V3 轻量总控内核
├── agents/openai.yaml       # Skill 展示和自动触发设置
├── docs/                    # 固定项目产物模板和门禁说明
├── references/              # 10 个按需专业模块
├── 总文档.md                 # 你正在看的完整人类说明
├── 说明.md                   # 快速说明
└── 规化全链路.md             # 旧文件名兼容入口
```

## V3 的变化

- 主 Skill 从约 1730 行缩到约 300 行。
- 37 个交叉重复参考合并为 10 个中文模块。
- 用户看到十步和当前动作，内部按路由加载专业细节。
- 固定模型名降级为 A0-A4 能力识别。
- 专业角色按风险激活，不让小项目背十个 Agent。
- 审查统一为卡片、成熟产品、超级测评三层。
- 文档按 R1-R4 缩放，一个事实只保留一个权威来源。
- 兼容旧项目，不删除旧文档、代码或数据。

## V3.1 的变化

- 新增开发前开源参考与复用门禁。
- R1 可轻量检查或记录跳过理由；R2-R4 在架构和编码前必须完成候选搜索与证据记录。
- 新增固定产物 `docs/02-开源参考与复用评估.md`。
- 补齐许可证、NOTICE、供应链、安全、版本锁定、SBOM/依赖清单、升级和退出方案规则。
- 增加全项目合法合规硬门禁：权利、授权、隐私、数据来源和适用规则不明时必须暂停。

## 边界

Skill 可以规划、执行、审查和交付，但不能保证产品一定赚钱、绝对无缺陷、第三方永远可用，也不能替代法律、医疗、财务等专业意见。遇到外部权限、平台、数据和商业未知时，会说清证据、风险和最小验证。
