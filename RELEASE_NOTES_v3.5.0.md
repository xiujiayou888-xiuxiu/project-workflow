# workflow-kit v3.5.0 发布说明

## 本版变化

- 默认交付路线调整为 `基础版 -> 进阶版 -> 高阶版 -> 最终版`。
- 基础版定义为真实、可用、可验收的核心闭环，不使用 Demo 作为阶段或交付等级。
- PRD、架构、功能卡、并行调度和状态模板均记录当前阶段，并只创建当前阶段的需求和任务。
- 当前阶段完成后生成 `docs/20-阶段复盘与升版决定.md`；没有上一阶段证据和用户确认时，不自动派发下一阶段。
- `R1-R4` 继续只控制风险与治理强度，阶段较小不豁免适用的安全、合规、审批和 POC 门禁。

## 兼容与边界

- 旧项目的 `SLICE-001` 标识继续兼容，表示基础版首个真实闭环；新项目使用 `BASE-001`。
- 不删除旧项目文档、代码、数据、配置或已发布版本。
- 已创建公开 Git tag `v3.5.0` 与 GitHub Release，并上传已校验的安装包；未申请账号或付费权限。

## 安装包内容

`project-workflow-skill.zip` 仅包含可安装的 Skill 文件：根 `SKILL.md`、Agent 配置、开发前复用评估模板和 10 个按需加载的参考模块。

## 发布前验证

- 校验 `SKILL.md` 元数据为 `3.5.0`，并检查全部引用存在。
- 验证基础版不会自动扩展到进阶版，阶段复盘模板包含验收证据与用户决定。
- 重新打包后，逐文件核对源码与 zip 内可安装内容的 SHA256。
- 已完成发布复验：tag、Release 和安装包资产均已存在；资产 SHA256 为 `39ED3A01A2D4AB89FCA39D937D4998F970460B9848DD2EFD8ABD9C8A509511E7`。
