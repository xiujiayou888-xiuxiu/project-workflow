# 示例：自动P图软件 — 全链路地图

> 以下模拟"规化全链路"收到"我要做一个自动P图软件"后的实际输出。

## 🔍 自动扫描结果

- 终端 AI：ZCode + Claude Code
- IDE：Cursor
- 桌面 APP：GitHub Desktop + Claude 桌面版
- 命令行工具：Git v2.47 + Docker v29.5 + Node.js v24
- 项目管理工具：Workbuddy
- 浏览器工具：v0.dev + Vercel + Railway

## 📋 自动P图软件 — 10 步开发地图

### STEP 1 — 理清需求

- 打开：ZCode
- 输入：

  ```
  我有一个想法：自动P图软件。
  用户上传图片，AI自动识别需要修什么（去背景/调色/去水印/人像美化），一键出图。
  目标用户是电商卖家、社交媒体运营、不想学PS的普通人。
  帮我理清需求边界和MVP范围。
  ```

- 完成标志：能一句话说清谁用、解决什么、MVP包含哪几个功能

### STEP 2 — 拆功能卡片

- 打开：Workbuddy
- 输入：

  ```
  基于这个MVP：用户上传图片→AI处理→下载成品图。
  拆成3张功能卡片，每张DB→API→页面一条龙，按优先级排列。
  ```

- 预计拆法：
  1. 上传图片（用户上传 → 存云存储 → 预览）
  2. AI处理（选模式 → 调AI → 返回结果）
  3. 下载分享（下载成品图 / 生成分享链接）

- 完成标志：看板里3张卡片按优先级排好

### STEP 3 — 技术方案

- 打开：ZCode
- 输入：

  ```
  我要做自动P图SaaS，用户上传图片→AI处理→出图下载。Web端。
  帮我出技术栈选型、目录结构、数据库表设计。
  ```

- 完成标志：知道技术栈和第一个文件从哪写

### STEP 4 — 初始化项目

- 打开：ZCode（执行命令）
- 输入：

  ```
  帮我初始化Next.js项目，配Tailwind、TypeScript。git init，推到GitHub。
  ```

- 辅助：GitHub Desktop 看提交历史
- 完成标志：`npm run dev` 能看到页面

### STEP 5 — 逐功能编码

卡片1（上传图片）：
1. ZCode："上传功能涉及哪些文件？按什么顺序写？"
2. Cursor：写上传组件、API路由、存储逻辑
3. 遇跨文件bug → 切ZCode
4. 完成 → ZCode审查 → GitHub Desktop提交

卡片2、3同样循环。

- 铁律：单文件用Cursor，跨文件用ZCode
- 完成标志：三个功能都跑通

### STEP 6 — UI界面

- 打开：v0.dev（浏览器）
- 输入：

  ```
  一个图片处理页面。左侧上传区，中间选P图模式（去背景/调色/去水印/人像美化），
  右侧预览+下载按钮。风格简洁现代。
  ```

- 后续：复制代码 → 贴进Cursor接后端逻辑
- 完成标志：页面能上传→处理→下载

### STEP 7 — 写测试

- 打开：ZCode
- 输入：

  ```
  给图片上传API和AI处理模块写测试，覆盖正常上传、超大文件、不支持格式、AI调用失败。
  ```

- 完成标志：`npm test` 全绿

### STEP 8 — 最终审查

**主审** — ZCode：

```
逐条对照PRD审查：上传安全、AI调用容错、用户数据隔离、XSS防护、存储成本。
```

**交叉审** — Claude 桌面版：

```
Review for bugs and security issues.
```

- 完成标志：两边通过，严重问题修完

### STEP 9 — 封装+部署

1. ZCode："帮我写Dockerfile"
2. Docker Desktop：构建镜像
3. 前端→Vercel，后端→Railway

- 完成标志：手机能打开，上传图片拿到结果

### STEP 10 — 更新进度

- 打开：Workbuddy
- 操作：三张卡片标记完成，记录耗时
- 完成标志：项目闭环
