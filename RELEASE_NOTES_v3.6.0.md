# workflow-kit v3.6.0 发布说明

## 本版变化

- 当前版本的需求、范围、预算、架构和开工授权确认后，当前总控 Agent 与其可控子 Agent 连续完成已批准的开发、测试、总装和验收。
- 不再按普通步骤、功能卡、波次、测试或可控子 Agent 派发要求用户点击“下一步”。
- 再次确认仅限三类：高风险操作、边界或验收不清、执行中必须由用户决定的真实问题。
- 外部不可控 AI 需要用户复制交接包、切换工具或登录授权时，才在实际交接点确认。
- 状态和执行清单模板新增执行模式、执行主体与再次确认触发项。

## 边界

- 不削弱付费、账号、API Key、生产发布、数据迁移、删除及其他不可逆操作的独立确认要求。
- 当前版本完成后仍等待用户验收和升版决定，不会自动启动下一版本。
- `v3.5.0` 及其 Release 保持不变。

## 安装包

- 文件：`project-workflow-skill.zip`
- SHA256：`4B335AEA2A9BFFA1336C71C4F995BF1441C432398481CB5AB98F6AE040BE4615`
- 大小：`55502` 字节
- 内容：根 `SKILL.md`、Agent 配置、开发前复用评估模板和 10 个按需加载的参考模块。

## 发布验证

- `SKILL.md` 元数据：`3.6.0`。
- 13 个可安装文件与源码逐项 SHA256 一致。
- `git diff --check` 通过。
